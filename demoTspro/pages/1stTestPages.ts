import { browser, element, by, protractor, promise } from 'protractor';
export class FirstTest {

    public static firstMethod() {
        console.log("hello this console comes from page")
    }
}